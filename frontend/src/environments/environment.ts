export const environment = {
  production: false,
  /** Base de la API NestJS del SECAD Lite. */
  apiBaseUrl: 'http://localhost:3000/api',
  /** Tenant activo (municipio). En el SaaS real se resuelve por subdominio/login. */
  tenant: 'demo',
};
